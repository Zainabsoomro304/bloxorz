import * as THREE from 'three';
export type StarsProps = {
    radius?: number;
    depth?: number;
    count?: number;
    factor?: number;
    saturation?: number;
    fade?: boolean;
    speed?: number;
};
export declare class Stars extends THREE.Points {
    speed: number;
    constructor({ radius, depth, count, saturation, factor, fade, speed, }?: StarsProps);
    rebuildAttributes({ radius, depth, count, saturation, factor, fade, speed, }: StarsProps): void;
    update(elapsedTime: number): void;
}
